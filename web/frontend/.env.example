# Copy to .env.local for local development.
# On Vercel: Project Settings -> Environment Variables -> add the same key, then redeploy.

# Public HTTPS URL of your Django backend (required for production / mobile access)
VITE_API_BASE_URL=https://your-backend-host.com/api/
